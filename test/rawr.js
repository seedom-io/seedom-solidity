var chai = require('chai');
var assert = chai.assert;

suite('testSuite', (accounts) => {

    it("should reject cancel before first construct", async () => {
        assert.isOk(false);
    });
    it("should reject cancel before first construct", async () => {
        assert.isOk(false);
    });
    it("should reject cancel before first construct", async () => {
        assert.isOk(false);
    });
    it("should reject cancel before first construct", async () => {
        assert.isOk(true);
    });
    it("should reject cancel before first construct", async () => {
        assert.isOk(false);
    });

});